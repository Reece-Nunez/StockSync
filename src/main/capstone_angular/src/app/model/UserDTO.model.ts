export interface UserDTO {
  id: number;
  username: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  role: string;
  // Add any other fields that are returned by your API and that you want to use
}
